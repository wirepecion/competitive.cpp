#include <bits/stdc++.h>
using namespace std;

int main(){
    ios::sync_with_stdio(0);
    cin.tie(0);

    int n,k,l; cin>>n>>k>>l;

    set <int> s;
    int arr[k];
    for(int i = 0 ; i < n ;i++){
        s.insert(i);
    }

     next_permutation

}